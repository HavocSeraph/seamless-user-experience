{
  "name": "skill-barter",
  "version": "1.0.0",
  "private": true,
  "description": "SkillBarter - Peer-to-Peer Skill Exchange Platform",
  "scripts": {
    "dev": "pnpm -r run dev",
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint"
  }
}
